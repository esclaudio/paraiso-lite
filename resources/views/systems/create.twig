{% extends 'layouts/app.twig' %}

{% block js %}
    <script>
        $(function() {
            App.initForm();
        });
    </script>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ __('Creating System') }}
        </div>
        <div class="card-body">
            <form id="form" action="{{ path_for('systems.store') }}" method="post" autocomplete="off">
                {{ csrf_field() }}
                
                {% include 'systems/partials/form.twig' %}
            </form>
        </div>
    </div>
{% endblock %}
