{% extends 'layouts/app.twig' %}

{% block js %}
    <script>
        $(function() {
            App.initForm();
        });
    </script>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ __('Modifying Document Type') }}
        </div>
        <div class="card-body">
            <form id="form" action="{{ path_for('documents_types.update', {document_type: document_type.id}) }}" method="post" autocomplete="off">
                {{ csrf_field() }}
                
                <input type="hidden" name="_METHOD" value="PUT">

                {% include 'documents_types/partials/form.twig' %}
            </form>
        </div>
    </div>
{% endblock %}
