{% extends 'layouts/app.twig' %}

{% block js %}
    <script>
        $(function() {
            $('#form').ajaxForm();
        });
    </script>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header">
            {{ __('Creating Risk Type') }}
        </div>
        <div class="card-body">
            <form id="form" action="{{ path_for('risks_types.store') }}" method="post" autocomplete="off">
                {{ csrf_field() }}
                
                {% include 'risks_types/partials/form.twig' %}
            </form>
        </div>
    </div>
{% endblock %}
