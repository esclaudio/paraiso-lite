<li class="nav-item dropdown no-arrow">
    <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
        <i class="fa fa-bell fa-fw"></i>
        <span id="alerts-count" class="badge badge-danger badge-counter"></span>
    </a>
    <div id="alerts-items" class="dropdown-menu scrollable-menu dropdown-menu-right">
        <a class="dropdown-item" href="#">
            {{ __('Nothing to do. Enjoy the day!') }}
        </a>
    </div>
</li>

<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" data-toggle="dropdown">
        {{ auth.user.name }}
    </a>
    <div class="dropdown-menu dropdown-menu-right">
        <a class="dropdown-item" href="{{ path_for('logout') }}">
            {{ __('Logout') }}
        </a>
    </div>
</li>